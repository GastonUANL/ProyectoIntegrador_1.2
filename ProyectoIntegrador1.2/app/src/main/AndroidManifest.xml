<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectointegrador12">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoIntegrador12">
        <activity android:name=".principal.Registro"></activity>
        <activity android:name=".MiPerfil" />
        <activity
            android:name=".Administradores"
            android:label="Administradores"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity
            android:name=".VistaNoticia"
            android:label="Noticia"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity android:name=".MenuDinamico" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>